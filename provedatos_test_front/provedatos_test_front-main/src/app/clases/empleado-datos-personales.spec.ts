import { EmpleadoDatosPersonales } from './empleado-datos-personales';

describe('EmpleadoDatosPersonales', () => {
  it('should create an instance', () => {
    expect(new EmpleadoDatosPersonales()).toBeTruthy();
  });
});
